# Dipendenze agente ricerca ricambi truck
# Installa con: pip install -r requirements_agent.txt

# Server web e API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0

# Claude API con Computer Use
anthropic>=0.40.0

# Screenshot e annotazione immagini
Pillow>=10.0.0
opencv-python>=4.9.0

# Input injection mouse/tastiera (per RDP)
pyautogui>=0.9.54
pywin32>=306  # Solo Windows — per gestione finestre RDP

# Knowledge base
pyyaml>=6.0.1

# Già presenti in requirements.txt del progetto principale:
# google-generativeai (non richiesto qui)
